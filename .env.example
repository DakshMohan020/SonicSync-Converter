# SonicSync does not require any API keys or environment variables to run.
# All conversion is handled server-side via yt-dlp and ffmpeg.
# This file is provided as a template in case you add integrations in the future.

# Example (not used by default):
# NEXTAUTH_SECRET=your-secret-here
# DATABASE_URL=your-database-url-here
